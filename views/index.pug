extends layout
include _mixins
block body
  h2.mt-4.mb-3 Comparison of different frameworks
  table.table.table-striped
    thead
      tr
        th Name
        th 
          a(href="#" data-bs-toggle="tooltip" title="Github stars") Stars
        th
          a(href="#" data-bs-toggle="tooltip" title="Github forks") Forks
        th
          a(href="#" data-bs-toggle="tooltip" title="Language the framework is written or based on") Language
        th AJAX
        th History
        th Animation
        th Sockets
        th
          a(href="#" data-bs-toggle="tooltip" title="Server Sent Events?") SSE
        th
          a(href="#" data-bs-toggle="tooltip" title="Langauge or Framework Agnostic??") Agnostic
        th 
          a(href="#" data-bs-toggle="tooltip" title="Provides an extension mechanism for defining and using extensions?") Extendable
        th
          a(href="#" data-bs-toggle="tooltip" title="Is it dependent on any external packages?") Dependency Free
        th
          a(href="#" data-bs-toggle="tooltip" title="Internet Explorer 11 compatible") IE 11
        th
          a(href="#" data-bs-toggle="tooltip" title="Does it require any compilation or build pipeline?") Compilation
        th
          a(href="#" data-bs-toggle="tooltip" title="Can be consumed via CDN?") CDN
    tbody
      each lib in data
        tr
          td 
            a(href=lib.website) #{lib.name}
          td #{lib.github.stars}
          td #{lib.github.forks}
          td #{lib.language}
          td 
            +yesno(lib.ajax)
          td 
            +yesno(lib.history)
          td 
            +yesno(lib.animation)
          td 
            +yesno(lib.websockets)
          td 
            +yesno(lib.sse)
          td 
            +yesno(lib.agnostic)
          td 
            +yesno(lib.extendable)
          td 
            +yesno(lib.dependencyFree)
          td 
            +yesno(lib.ie11)
          td
            +yesno(lib.compilation)
          td
            +yesno(lib.cdn)
  .card
    .card-body
      figure.text-center
        blockquote.blockquote
          p The future of web-based software architectures is already taking form, and this time it’s server-rendered (again). Papa’s got a brand new bag: HTML-over-WebSockets and broadcast everything all the time.
        figcaption.blockquote-footer
          | Matt E. Patterson in <cite title="Source Title"><a href="https://alistapart.com/article/the-future-of-web-software-is-html-over-websockets/">The Future of Web Software is HTML-over-WebSockets</a></cite>
